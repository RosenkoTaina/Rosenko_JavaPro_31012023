In Java, the array object is used to store multiple elements of the same type.
These elements are stored in contiguous memory, the length of which is immutable once initialization is complete.
We can declare an array in the same way as any other variable or object in Java, by giving a type and a name: